backend:
name: git-gateway
branch: main

Local onde as imagens serão salvas
media_folder: "imagens"

Como as imagens serão acessadas no site
public_folder: "/imagens"

Coleções de conteúdo gerenciáveis
collections:

name: "site_data"
label: "Dados do Site"
files:

name: "portfolio"
label: "Portfólio"
file: "_data/portfolio.json"
description: "Gerencie os itens que aparecem na seção de portfólio."
fields:

name: "items"
label: "Itens do Portfólio"
label_singular: "Item do Portfólio"
widget: "list"
fields:

{label: "Título", name: "title", widget: "string", required: true}

{label: "Categoria", name: "category", widget: "select", options: ["residencial", "comercial"]}

{label: "Imagem", name: "image", widget: "image", required: true}
